What this folder would contain ----

This folder would contain 7 txt files with order of each round. An example of Round 1 is added. 
It would also contain csv files of the draftable players. An example of the FS position is added.
Only those 2 examples are added, since there is not a lot of space to include all the data. 
